readonly SCRIPT_DIR="$(dirname "${BASH_SOURCE[0]}")"

function start_registry() {
    echo "start_registry"
    echo "$SCRIPT_DIR"
    echo "TODO: delete me"
    return 0
}

function stop_registry() {
    echo "stop_registry"
    echo "$SCRIPT_DIR"
    echo "TODO: delete me"
    return 0
}
